# 작업용 원본 이미지 — 페이지에는 data URI 로 임베드되므로 저장소에 둘 필요가 없다.
# 공개 저장소라 원본이 그대로 올라가는 것도 피한다.
*.png
*.jpg
*.jpeg
*.webp
*.heic

# 단, 사이트가 실제로 서빙하는 자산은 커밋한다(og:image 등은 URL 이 필요하다)
!assets/
!assets/**

# macOS
.DS_Store
